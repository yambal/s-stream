{
  "name": "s-stream",
  "version": "1.0.0",
  "description": "Icecast-compatible streaming server with admin UI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
